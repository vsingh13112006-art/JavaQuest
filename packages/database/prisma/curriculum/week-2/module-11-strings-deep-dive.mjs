import { buildModule, mainProgram } from "./module-builder.mjs";

const p = (body) => mainProgram(body);
const test = (expectedOutput) => [{ expectedOutput, isHidden: false }];
const item = (slug, title, description, why, model, example, answer, body, solution, output, difficulty) => ({
  slug, title, description, why, model, example,
  predict: [`${slug}-prediction`, `Predict: ${title}`, answer[0], answer[1]],
  code: [`${slug}-practice`, `Practice: ${title}`, description, p(body), p(solution), test(output)], difficulty,
});

const items = [
  item("strings-indexed-data", "Strings Are Indexed Data", "String ke characters ko indexed sequence ki tarah read karo.", "Array ke baad String indexing familiar lagegi: first character ka index bhi 0 hai.", "index 0 1 2 3\ntext  J a v a", "~~~java\nString word=\"Java\";\nchar first=word.charAt(0);\n~~~", ["`Java` ka index 2 character kya hai?", "v"], "    String word = \"Java\";\n    // Print first and last characters", "    String word = \"Java\";\n    System.out.println(word.charAt(0));\n    System.out.println(word.charAt(3));", "J\na"),
  item("strings-length-method", "String length()", "String character count `.length()` se print karo.", "Array ki `length` property aur String ka `length()` method intentionally distinguish karo.", "array.length ≠ text.length()", "~~~java\nString name=\"Aman\";\nSystem.out.println(name.length()); // 4\n~~~", ["`JavaQuest`.length() kya hai?", "9"], "    String language = \"Java\";\n    // Print Length: value", "    String language = \"Java\";\n    System.out.println(\"Length: \" + language.length());", "Length: 4"),
  item("strings-char-at", "Read with charAt()", "Dynamic indices se selected characters access karo.", "`charAt(index)` exactly one `char` return karta hai; valid range 0 to length-1 hai.", "text + index → char", "~~~java\nchar last = text.charAt(text.length()-1);\n~~~", ["`Code`.charAt(1) kya hai?", "o"], "    String word = \"Quest\";\n    // Use length to print last char", "    String word = \"Quest\";\n    System.out.println(word.charAt(word.length() - 1));", "t"),
  item("strings-equals-comparison", "Compare with equals()", "String content ko `.equals()` se correctly compare karo.", "`==` references compare kar sakta hai; learner input ka content compare karne ke liye `.equals()` use karo.", "text.equals(expected) → content comparison", "~~~java\nif(name.equals(\"Aman\")) { ... }\n~~~", ["Correct String comparison method exactly enter karo: equals", "equals"], "    String name = new String(\"Aman\");\n    // Print Match when content matches", "    String name = new String(\"Aman\");\n    if (name.equals(\"Aman\")) System.out.println(\"Match\");", "Match"),
  item("strings-ignore-case", "Compare Ignoring Case", "Case-insensitive matching ke liye `equalsIgnoreCase()` use karo.", "User `JAVA`, `Java` ya `java` type kar sakta hai; meaning same ho to case ignore karna practical hai.", "JAVA ≈ java when case ignored", "~~~java\nanswer.equalsIgnoreCase(\"yes\")\n~~~", ["`JAVA`.equalsIgnoreCase(`java`) result kya hai?", "true"], "    String answer = \"YES\";\n    // Print Accepted", "    String answer = \"YES\";\n    if (answer.equalsIgnoreCase(\"yes\")) System.out.println(\"Accepted\");", "Accepted"),
  item("strings-change-case", "Change Letter Case", "`toUpperCase()` aur `toLowerCase()` se normalized text banao.", "Case methods original String ko mutate nahi karte; transformed String return karte hain.", "original → method → new normalized text", "~~~java\nString lower=name.toLowerCase();\n~~~", ["`Java`.toUpperCase() output kya hai?", "JAVA"], "    String skill = \"JaVa\";\n    // Print lower then upper", "    String skill = \"JaVa\";\n    System.out.println(skill.toLowerCase());\n    System.out.println(skill.toUpperCase());", "java\nJAVA"),
  item("strings-trim-input", "Clean Outer Spaces", "`trim()` se leading/trailing spaces remove karo.", "User input me accidental outer spaces comparison ko fail kara sakte hain. Cleaning pehle karo.", "raw input → trim → clean input", "~~~java\nString clean=raw.trim();\n~~~", ["`  Java  `.trim() exactly kya deta hai?", "Java"], "    String raw = \"  Java Quest  \";\n    // Print inside brackets after trim", "    String raw = \"  Java Quest  \";\n    System.out.println(\"[\" + raw.trim() + \"]\");", "[Java Quest]"),
  item("strings-substring", "Extract with substring()", "Start-inclusive/end-exclusive range se String part nikalo.", "`substring(start,end)` start ko include aur end ko exclude karta hai.", "JavaQuest → [0,4) → Java", "~~~java\nword.substring(0,4)\n~~~", ["`Developer`.substring(0,3) kya hai?", "Dev"], "    String word = \"JavaQuest\";\n    // Print Java and Quest", "    String word = \"JavaQuest\";\n    System.out.println(word.substring(0, 4));\n    System.out.println(word.substring(4));", "Java\nQuest"),
  item("strings-contains-challenge", "Inspect with contains()", "Clean, normalize aur inspect methods ko combine karo.", "Search se pehle case normalize karna user text ko predictable banata hai.", "raw → trim → lowercase → contains", "~~~java\nclean.toLowerCase().contains(\"java\")\n~~~", ["`I love Java`.contains(`Java`) result kya hai?", "true"], "    String message = \"  I LOVE JAVA  \";\n    // Normalize and print Contains Java: true", "    String message = \"  I LOVE JAVA  \";\n    String clean = message.trim().toLowerCase();\n    System.out.println(\"Contains Java: \" + clean.contains(\"java\"));", "Contains Java: true", "INTERMEDIATE"),
];

export const stringsDeepDiveModule = buildModule({
  slug: "week-2-strings-deep-dive", title: "Week 2 — Strings Deep Dive",
  description: "String indexing, comparison, normalization, extraction aur inspection confidently use karo.", position: 11, items,
});
